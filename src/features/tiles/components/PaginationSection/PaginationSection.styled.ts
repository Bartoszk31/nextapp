import { styled } from '@mui/material/styles'

const PaginationSectionWrapper = styled('div')`
  margin-top: ${({ theme }) => theme.spacing(2)};
  display: flex;
  justify-content: center;
`

export {
  PaginationSectionWrapper
}
